import type { Theme } from '@/types';

export const THEMES: readonly Theme[] = ['system', 'light', 'dark'];

export const WINDOW_BACKGROUND_LIGHT = '#FFFFFF';
export const WINDOW_BACKGROUND_DARK = '#191919';
